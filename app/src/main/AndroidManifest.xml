<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.w3engineers.core.snacksready">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name="com.w3engineers.core.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.splash.SplashActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/AppThemeSplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.main.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />

        <!-- Declare  Alarm Broadcast receiver here -->
        <receiver android:name="com.w3engineers.core.util.lib.alarm.AlarmReceiver" />

        <!-- Declare  Alarm Notification Service here -->
        <service android:name="com.w3engineers.core.util.lib.alarm.AlarmNotificationService" />

        <!-- Declare  Alarm Sound Service here -->
        <service android:name="com.w3engineers.core.util.lib.alarm.AlarmSoundService" />

        <activity android:name=".ui.home.HomeActivity"
            android:theme="@style/CustomTheme"/>
    </application>

</manifest>